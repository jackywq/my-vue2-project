# Vue 2.x + TypeScript Best Practices Demo

这是一个基于 Vue 2.6 和 TypeScript 的全栈技术演示项目，旨在深入探讨 Vue 生态系统中的核心概念、组件通信模式、生命周期优化以及状态管理最佳实践。

项目集成了 **Vue Router**, **Vuex**, **Ant Design Vue**，并提供了多个针对特定技术难点的深度演示（Deep Dives）。

## 🛠 技术栈 (Tech Stack)

| 技术 | 说明 | 版本 |
| --- | --- | --- |
| **Vue.js** | 核心框架 | ~2.6.11 |
| **TypeScript** | 静态类型语言 | ~3.9.3 |
| **Vue Router** | 路由管理 | ^3.0.1 |
| **Vuex** | 状态管理 | ^3.4.0 |
| **Ant Design Vue** | UI 组件库 | ^1.6.4 |
| **Less** | CSS 预处理器 | ^3.0.4 |

## 🚀 核心演示模块 (Key Features)

### 1. 组件生命周期与执行顺序 (Lifecycle Execution Order)
**路径**: `src/views/ExecuteOrder`

深入探究父子组件在不同渲染策略下的生命周期钩子执行顺序。
- **场景 A (同步渲染)**: 展示默认行为，子组件 `created` -> `mounted` 优先于父组件 `mounted`。
- **场景 B (异步/条件渲染)**: 展示使用 `v-if` 配合父组件数据请求，实现“父组件数据就绪 -> 子组件挂载”的优化流程，解决子组件依赖父组件异步数据的问题。

### 2. 事件总线通信 (EventBus)
**路径**: `src/views/EventBus`

演示在非父子组件（如兄弟组件）之间使用全局 `EventBus` 进行解耦通信。
- **Sender**: 发送事件。
- **Receiver**: 监听并接收事件。

### 3. Vuex 状态管理深度实践 (Vuex State Management)
**路径**: `src/views/VueXState`

完整展示 Vuex 3.x 的核心功能与最佳实践：
- **核心概念**: State, Getters, Mutations, Actions 的使用。
- **辅助函数**: `mapState`, `mapGetters`, `mapMutations`, `mapActions` 的 TypeScript 写法。
- **模块化**: 集成 `ModuleA` 展示大型应用的状态切分。

### 4. 路由配置与优化 (Router Configuration)
**路径**: `src/router/index.ts`
- 包含路由懒加载配置（示例）。
- **Bug Fix**: 修复 Vue Router 3.x 中常见的 `Avoided redundant navigation` 报错问题。

## 📂 项目结构 (Project Structure)

```text
src/
├── assets/          # 静态资源
├── router/          # 路由配置 (含全局拦截器与错误处理)
├── store/           # Vuex 状态管理
│   ├── index.ts     # Store 入口
│   └── moduleA.ts   # 模块化状态示例
├── utils/           # 工具库
│   └── eventBus.ts  # 全局事件总线实例
├── views/           # 页面级组件
│   ├── EventBus/    # [演示] 事件总线通信
│   ├── ExecuteOrder/# [演示] 父子组件生命周期顺序
│   ├── VueXState/   # [演示] Vuex 状态管理
│   ├── Home/        # 首页
│   └── VueRoute/    # 路由演示
├── App.vue          # 根组件
├── main.ts          # 应用入口
├── shims-tsx.d.ts   # TSX 类型定义
└── shims-vue.d.ts   # Vue 类型定义
```

## ⚡ 快速开始 (Quick Start)

### 安装依赖
```bash
npm install
```

### 启动开发环境
```bash
npm run serve
```

### 构建生产版本
```bash
npm run build
```

### 代码检查与修复
```bash
# 运行 ESLint
npm run lint

# 自动修复 ESLint 错误
npm run fix

# 格式化代码 (Prettier)
npm run format
```

## 📄 License
[MIT](LICENSE)
